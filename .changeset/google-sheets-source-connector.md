---
'owox': minor
---

# Google Sheets source connector

Add a Google Sheets source connector that imports one sheet into a table in the user's Storage and makes it available as a Data Mart. The connector supports Service Account authentication, dynamic header-based field inference, and full refreshes across supported storages so the materialized table reflects the current sheet contents, including schema changes.
